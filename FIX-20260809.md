# 桑多涅状态面板 v1.2.0 修复说明（2026-08-09）

## 背景
Hana 升级到 0.446.6 后，插件在**切换会话时卡死**（页面无响应、设置内容假缺失），重启 Hana 才能恢复。

## 根因
插件状态面板使用 **SSE 长连接**（`/api/status-stream` + `EventSource`）推送状态更新。
Hana 0.446.6 切换会话时，插件 widget/card iframe 会被**风暴式重挂载**（一次切换开 5~50 个 iframe），
每个 iframe 都建立一条 SSE 长连接，且旧连接的 abort 事件**偶发不触发**，导致连接数只增不减
（实测从 1 一路累积到 50+），最终拖垮 Hana。

## 修复方案：SSE 长连接 → 短轮询
`status.json` 只有几百字节，短轮询开销可忽略，且不产生长连接累积问题。

### 改动文件

#### 1. `routes/ui.js`
- **删除** `/api/status-stream`（SSE 路由）
- **删除** `sseClients` 集合、`fs.watch(statusPath)` 广播、SSE 清理定时器
- **删除** widget HTML 里的 `EventSource` 连接（原来监听 `refresh` 事件刷新状态）
- **删除** `serveFile` 里对 widget.bundle.js 的 `setInterval(loadStatus, 5e3)` 移除逻辑（原为 SSE 让路）
- **删除** `import { TextEncoder } from "node:util"`（不再使用）
- **保留** `/api/status` 轮询接口不变
- widget HTML 里改为 `setInterval(function () { loadStatus(); }, 3000)`
- 新增注释说明改造原因

#### 2. `assets/widget.bundle.js`
- 文件末尾追加 `setInterval(loadStatus, 4000)` 短轮询

#### 3. 额外修复：会话切换列表隐藏微信/QQ 连接（用户要求）
`routes/ui.js` 的 `/api/agent-sessions` 路由：
- 原来：bridge/phone 会话路径加"微信 · "前缀显示
- 现在：`if (/bridge|phone/.test(fp)) continue;` 直接跳过，**不在设置面板显示**
- 作用：设置里会话切换不再展示微信/QQ 外部连接，只显示本地会话
- 注意：注入目标选择（trigger-reply.js）不受影响，仍可正常向 bridge 会话发消息

## 验证结果
- 短轮询方案部署后，diag 日志显示 **sse-add 不再增长**，纯轮询请求正常
- 连续切换会话 5-10 次**不再卡死**
- 4 个文件语法检查全部通过（node --check）

## 变更文件清单
| 文件 | 变更 |
|------|------|
| `routes/ui.js` | SSE 删改 + 短轮询 + bridge 过滤 |
| `assets/widget.bundle.js` | 追加轮询 |
| `index.js` | 无变更（与发布版一致） |
| `routes/trigger-reply.js` | 无变更（与发布版一致） |

## 附带说明
- 诊断用的 diag 埋点（lib/diag.js）已全部移除，未进入发布版
- 发布包：`san-status-panel-v1.2.0-fixed.zip`
